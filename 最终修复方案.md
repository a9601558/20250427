# JSON题库上传 - 最终修复方案

## 🔍 问题诊断结果

通过分析错误日志，发现了**两个关键问题**：

### 问题 1: questionSetId 为 undefined
```
[JSON-API] 创建题库成功, ID: undefined
```

**原因：**代码尝试插入 `questionCount` 字段，但该字段在 QuestionSet 模型中不存在，导致创建失败或返回不完整的对象。

### 问题 2: SQL 占位符参数不匹配
```
Error: Positional replacement (?) 1 has no entry in the replacement map
```

**原因：**SQL 语句中使用 `NOW()` 函数，但 replacements 数组中没有对应参数。

---

## ✅ 已修复的内容（本地代码）

### 修复 1: 移除不存在的 questionCount 字段
```typescript
// 修改前
const newQuestionSet = await QuestionSet.create({
  title,
  description,
  category,
  isPaid: isPaid === 'true' || isPaid === true,
  price: parseFloat(price) || 0,
  trialQuestions: parseInt(trialQuestions) || 0,
  questionCount: 0,  // ❌ 这个字段不存在
  isFeatured: false
}, { transaction });

// 修改后
const newQuestionSet = await QuestionSet.create({
  title,
  description,
  category,
  icon: 'default',  // ✅ 添加必需字段
  isPaid: isPaid === 'true' || isPaid === true,
  price: parseFloat(price) || 0,
  trialQuestions: parseInt(trialQuestions) || 0,
  // ✅ 移除 questionCount
  isFeatured: false
}, { transaction });
```

### 修复 2: 改进 ID 获取方式
```typescript
// 修改前
const questionSetId = (newQuestionSet as any).id;

// 修改后
const questionSetId = newQuestionSet.get('id') || 
                      (newQuestionSet as any).dataValues?.id || 
                      (newQuestionSet as any).id;
```

### 修复 3: SQL 占位符参数匹配
```typescript
// 添加 const now = new Date();
// 将 NOW(), NOW() 改为 ?, ?
// 在 replacements 数组末尾添加 now, now
```

---

## 🚀 部署到生产服务器

### 方法 1: 使用 Git（推荐）

```bash
# 本地提交
cd /Users/wilson/Desktop/montopi/20250427
git add server/src/controllers/questionController.ts
git commit -m "修复JSON上传：移除questionCount字段，修复SQL占位符"
git push

# 服务器拉取并重启
ssh your-user@your-server
cd /www/wwwroot/root/git
git pull
cd server
npm run build
pm2 restart exam-server
```

### 方法 2: 直接上传编译后的文件

```bash
# 传输编译后的 JS 文件
scp /Users/wilson/Desktop/montopi/20250427/server/dist/controllers/questionController.js \
    your-user@your-server:/www/wwwroot/root/git/dist/server/dist/controllers/

# SSH 登录并重启
ssh your-user@your-server
pm2 restart exam-server
```

### 方法 3: 在服务器上直接修改源码

```bash
# SSH 到服务器
ssh your-user@your-server

# 编辑源文件
cd /www/wwwroot/root/git/server/src/controllers
nano questionController.ts

# 找到约第617行，按照上述"修复1"和"修复2"进行修改
# 然后保存

# 重新编译
cd /www/wwwroot/root/git/server
npm run build

# 重启服务
pm2 restart exam-server
```

---

## 🔍 验证部署

### 1. 检查服务器日志
```bash
pm2 logs exam-server --lines 50
```

应该看到题库ID不再是 undefined

### 2. 重新上传 JSON 文件

刷新浏览器，进入管理页面 → JSON题库导入，上传文件

### 3. 检查结果

应该显示：
```
✅ 成功导入: 217 道题目
失败: 0 道题目
```

---

## 📋 修复文件位置

本地修复后的文件：
- 源代码：`/Users/wilson/Desktop/montopi/20250427/server/src/controllers/questionController.ts`
- 编译后：`/Users/wilson/Desktop/montopi/20250427/server/dist/controllers/questionController.js`

需要同步到服务器：
- 服务器源码：`/www/wwwroot/root/git/server/src/controllers/questionController.ts`
- 服务器编译：`/www/wwwroot/root/git/dist/server/dist/controllers/questionController.js`

---

## 🎯 关键修改总结

1. ✅ 移除 `questionCount: 0` （字段不存在于模型）
2. ✅ 添加 `icon: 'default'` （必需字段）
3. ✅ 改进 `questionSetId` 获取方式
4. ✅ 添加 `const now = new Date();`
5. ✅ SQL 占位符从 6 个改为 8 个（Questions 表）
6. ✅ SQL 占位符从 5 个改为 7 个（Options 表）
7. ✅ replacements 数组添加 `now, now` 参数

---

## 📞 下一步

选择上述**方法1、2或3**之一部署到服务器，然后测试上传功能！
