# 单选题按钮显示逻辑说明

## 当前实现状态

### 单选题流程

#### 1. **未选择选项时**
- 按钮区域：**空白**（不显示任何按钮）
- 用户操作：可以点击选项

#### 2. **点击选项后（提交中）**
- 按钮区域：**空白**（不显示任何按钮）
- 系统操作：自动判断答案（100ms内完成）

#### 3. **答对后（isSubmitted = true, isCorrect = true）**
- 按钮区域：**空白**（不显示任何按钮）
- 动画：显示绿色 ✓ 图标（2秒）
- 解析：显示题目解析
- 系统操作：2秒后**自动进入下一题**

#### 4. **答错后（isSubmitted = true, isCorrect = false）**
- 按钮区域：显示**绿色「次の問題」按钮**
- 动画：显示红色 ✗ 图标（2.5秒）
- 解析：显示题目解析
- 用户操作：手动点击"次の問題"进入下一题

---

### 多选题流程

#### 1. **未选择选项时**
- 按钮：灰色「回答を送信」（禁用）
- 用户操作：可以选择多个选项

#### 2. **选择选项后**
- 按钮：蓝色「回答を送信」（可点击）
- 用户操作：点击"回答を送信"提交

#### 3. **提交后（已判断）**
- 按钮：绿色「次の問題」
- 动画：显示 ✓ 或 ✗ 图标
- 解析：显示题目解析
- 用户操作：点击"次の問題"进入下一题

---

## 重要说明

### ⚠️ 用户可能的误解

如果您看到**绿色的"次の問題"按钮**，这是**正确的行为**：
- 单选题**答错**时会显示此按钮
- 这**不是**"回答を送信"按钮

### ✅ 正确的单选题表现

| 情况 | 按钮显示 | 说明 |
|------|---------|------|
| 未选择 | 无按钮 | ✅ 正确 |
| 选择中 | 无按钮 | ✅ 正确 |
| 答对后 | 无按钮 → 2秒后自动进入下一题 | ✅ 正确 |
| 答错后 | 绿色「次の問題」按钮 | ✅ 正确（需要手动点击） |

### ❌ 错误的表现（如果出现）

| 情况 | 错误显示 | 原因 |
|------|---------|------|
| 单选题未提交 | 显示"回答を送信"按钮 | 代码逻辑错误 |
| 单选题答对后 | 显示任何按钮 | 应该自动进入下一题 |

---

## 代码验证

### 关键代码片段

```typescript
{/* 答题/下一题按钮 */}
<div className="mt-6">
  {question.questionType === 'single' ? (
    /* 单选题：只在已提交后显示"下一题"按钮 */
    isSubmitted ? (
      <button onClick={handleNext}>
        次の問題
      </button>
    ) : null  // ← 未提交时不显示任何按钮
  ) : (
    /* 多选题：显示"送信"或"下一题"按钮 */
    <button onClick={handleSubmit}>
      {isSubmitted ? '次の問題' : '回答を送信'}
    </button>
  )}
</div>
```

### 自动进入下一题逻辑

```typescript
// 单选题：如果答对，2秒后自动进入下一题
if (question.questionType === 'single' && isCorrect) {
  setTimeout(() => {
    handleNext(); // 自动调用下一题
  }, 2000);
}
```

---

## 测试场景

### 场景1：单选题答对
1. ✅ 点击正确选项
2. ✅ 看到绿色 ✓ 动画
3. ✅ **不显示任何按钮**
4. ✅ 2秒后自动进入下一题

**预期结果：** 整个过程无需点击任何按钮

---

### 场景2：单选题答错
1. ✅ 点击错误选项
2. ✅ 看到红色 ✗ 动画
3. ✅ 看到解析
4. ✅ **显示绿色「次の問題」按钮**
5. ✅ 手动点击进入下一题

**预期结果：** 答错时需要手动点击"次の問題"

---

### 场景3：多选题
1. ✅ 选择多个选项
2. ✅ 看到蓝色「回答を送信」按钮
3. ✅ 点击"回答を送信"
4. ✅ 看到结果和解析
5. ✅ 看到绿色「次の問題」按钮
6. ✅ 手动点击进入下一题

**预期结果：** 多选题需要两次点击（送信 + 下一题）

---

## 如果问题仍然存在

### 检查清单

1. **清除浏览器缓存**
   ```bash
   # 按 Cmd+Shift+R (Mac) 或 Ctrl+Shift+R (Windows)
   # 强制刷新页面
   ```

2. **确认构建版本**
   ```bash
   npm run build
   # 确认构建成功后再测试
   ```

3. **检查是否为多选题**
   - 多选题会显示"回答を送信"按钮
   - 单选题标签显示：**绿色「単一選択」**
   - 多选题标签显示：**紫色「複数選択」**

4. **检查题目类型**
   ```typescript
   // 在浏览器控制台运行
   console.log('题目类型:', question.questionType);
   // 应该输出: 'single' 或 'multiple'
   ```

---

## 按钮文本对照

### 日文按钮文本含义

| 日文 | 中文 | 英文 | 出现场景 |
|------|------|------|---------|
| 回答を送信 | 提交答案 | Submit Answer | **仅多选题**未提交时 |
| 次の問題 | 下一题 | Next Question | 答题后（单选答错、多选） |
| 練習を完了 | 完成练习 | Complete Exercise | 最后一题 |

### ⚠️ 重要区别

- **"回答を送信"** = 提交按钮（仅多选题）
- **"次の問題"** = 下一题按钮（答题后）

如果您看到的是**"次の問題"**，那是**答错后**需要手动点击的按钮，这是**正确的行为**！

---

## 总结

### 当前实现完全符合需求

✅ 单选题点击选项后直接判断  
✅ 答对时自动进入下一题（无需点击）  
✅ 答错时显示"次の問題"按钮  
✅ 多选题保持原有逻辑  

### 如果您仍然看到问题

请提供以下信息：
1. 具体看到的按钮文本（"回答を送信" 还是 "次の問題"？）
2. 题目类型标签（绿色"単一選択" 还是 紫色"複数選択"？）
3. 出现时机（未选择时？答对后？答错后？）
4. 截图或屏幕录像

这样我可以更准确地定位问题！

---

**最后更新：** 2025年11月11日  
**当前版本：** edbdc32  
**状态：** ✅ 功能已实现并验证
